const hre = require("hardhat");
const { expect } = require("chai");
const { airdropWant } = require("../scripts/utils");

describe("Beefy Battles Event", () => {
    before(async () =>{

    });

    it("Deposits want", async () => {

    });
})